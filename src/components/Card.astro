---
export interface Props {
  dark?: boolean;
}

const { dark = false } = Astro.props as Props;
---

<div
  class={`card p-5 rounded-md shadow-md mb-5 ${dark && 'bg-black text-white'}`}
>
  <slot />
</div>
